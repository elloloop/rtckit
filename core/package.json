{
  "name": "@elloloop/rtckit-core",
  "version": "0.0.1",
  "private": true,
  "description": "Headless, framework-agnostic real-time calling core (signaling protocol, transport policy, mesh/SFU contracts, headless event surface). Part of elloloop/rtckit.",
  "license": "Apache-2.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test \"dist-test/**/*.test.js\"",
    "clean": "rm -rf dist dist-test"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
