---
"@elloloop/rtckit-core": minor
---

Initial public release of the headless real-time calling core: signaling
protocol version + type guards, transport policy (mesh↔SFU decision and
thresholds), transport/media contracts, and the headless event surface.
Framework-agnostic, zero runtime dependencies. Published as dual ESM/CJS with
type declarations and npm provenance.
